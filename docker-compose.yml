version: '3.8'
services:
  mongo:
    image: mongo:7
    container_name: taxi-mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: taxi-logs
    restart: unless-stopped

volumes:
  mongo_data:
